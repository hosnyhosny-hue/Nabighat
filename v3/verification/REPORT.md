# Full v3 branch integration — verification record

Date: 2026-09-18

## Outcome
The full playable preview source is integrated on `feature/global-expedition-v3`, not merely the previous domain primitives. `main` remains `334a12c4dc484bae1633bfbb4aa90c12b3e0500b`. No merge, Pages settings change or deployment was performed.

Source commit tested: `2d0013c8a8d3ff39649a55e0021fed74ac0d2629`
Tree: `e3c285e70182e9224ae3e741f1bdd2a7bbd04ccf`
Built HTML: 435867 bytes
SHA-256: `1d2aa8dae91948da9f20c1a76733c9f9eb1221c8f063e2bb6813cd054bd1a1ba`
Browser test script Git blob: `d0d6e199b46133c6772177bfd13925e5080441c7`

## Source identity and build
All 13 build/test input files were matched by Git blob SHA-1 against the commit tree returned by the authenticated GitHub connector. The root Jekyll entrypoint was also read back and matched as blob `1908eb55098658f548ffb1766e049e33de9db5e5`. This was a verified reconstruction of the branch snapshot; it was not a successful network git clone.

The offline build passed the preserved 2.2.2 source check, the exact 2.2.3 mobile-parent check and the final v3 check. `--export-source` materialized the full editable renderer, character, dialogue, game, expedition, CSS and the three existing approved character illustrations. Rebuilding from that exported source produced the identical HTML hash. JavaScript syntax checks passed for every complete extracted JS module. The four expedition fragments are concatenated before syntax checking.

Compared with the previously delivered full preview (`31b4084bea0074a28d99fc3816c162b93b646dffa06bc34f5da5ca1a544953a1`), the integrated HTML differs by one insignificant whitespace character in a pointer handler. No gameplay feature was removed or replaced.

## Fresh test results
- **67 / 67 browser checks passed.**
- **6 / 6 independent Node domain tests passed.**
- **0 uncaught JavaScript exceptions** in the browser suite.
- Full-source export/rebuild round-trip: passed.
- Syntax checks: passed.

Browser checks covered the unchanged production HTML with real animation frames, keyboard movement, pause, circuit movement and return to menu; the conditional test APIs were absent in normal play. Deterministic checks then covered key pickup/return, contextual builder buttons, undo, oversized-piece rejection, both 3+3+2 and 4+4 bridge solutions, physically walking the bridge, Noor's pump role, water connectivity, oasis bloom, journal, safe respawn, invalid-save atomicity, save serialization/restore, hovercraft steering/hop/boost, paused timing, all 12 checkpoints, actual recorded replay samples, ghost replay, separate biome/assist records, untimed mode, local route import/rejection, GLB metadata inspection and all nine legacy thinking gates.

Mobile layout/input cases used 393x852 and 360x720 portrait Arabic, and 852x393 landscape English. The controls fit the viewport, the compact mission panel left the lower scene visible, touch jump worked, help paused play and language switching worked. Screenshots were visually reviewed for portrait gameplay and the circuit.

Synthetic original JSON route and GLB fixtures were generated by the test script. No racing-archive assets or track coordinates were uploaded or required.

## Exact test environment and limitations
Chromium 144.0.7559.96 with ANGLE SwiftShader/WebGL and Xvfb. The renderer was real, not a mocked canvas. Local HTTP navigation was blocked by the environment's browser policy, so the identical branch-built HTML was loaded in an allowed `about:blank` document. No browser policy was changed or bypassed.

The first production smoke test used actual animation frames, no source rewrites, no test helpers and no simulated storage; the game's real opaque-origin storage-unavailable fallback was exercised. Deterministic scenarios used `about:blank?test`, the game's existing conditional helpers and an explicitly simulated storage object. The source bytes were not altered. These tests do **not** establish real-origin persistent storage or hosted-URL operation.

Optional IBM font requests were blocked during this harness; fallback typography was used in screenshots. No Arabic recording or pronunciation was approved. No physical phone, child usability, battery, hardware-performance or store-readiness test was performed.

## GitHub Actions is not green
Read-only validation run `35354602295` for the source commit ended in `failure`. Its job `105630751087` reports **zero steps**, `runner_id: 0`, and no runner name. The exact root cause is not established by available metadata. No build, Node, Jekyll or artifact-upload step ran in that CI job. Do not describe this as a successful CI run or a game-code test failure.

The root Jekyll build/output comparison therefore remains **unverified by actual Jekyll**; only the locked offline assembly was executed successfully here. The workflow contains no deploy step, no Pages permission and no write token. PR #2 must remain draft until that separate validation/deployment decision is resolved.

## Reproduce
```sh
python3 v3/build.py --export-source
node --test v3/core.test.mjs
python3 _preview/source/build.py
cmp _preview/index.html _preview/source/index.html
python3 -m http.server 8080 --directory _preview
```
Open localhost:8080. For browser regression with Playwright and a configured Chromium display:
```sh
python3 v3/verify_browser.py --url http://localhost:8080
```
The recorded restricted-environment run instead explicitly used `--isolated`; see the script header. Neither command deploys or mutates a Git reference.
